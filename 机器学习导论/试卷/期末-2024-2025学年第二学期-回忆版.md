1. NFL
2. 下列哪个距离度量对异常值最敏感：
A. 曼哈顿距离    B. 欧氏距离    C. 余弦相似度    D. 马氏距离
3. 关于 Bagging 和 Boosting，下列描述正确的是

简答题：
一、
1. 什么是归纳偏好，解释奥卡姆剃刀原则
2. 什么是判别式模型和生成式模型，各举一到两个例子

二、线性模型
1. 简述 LDA 是如何应用到分类任务的？
2. LDA 的目标两个是什么？是如何体现在 $J=\dfrac{\boldsymbol{w}^T \boldsymbol{S}_b \boldsymbol{w}}{\boldsymbol{w}^T \boldsymbol{S}_w \boldsymbol{w}}$ 中的。

三、GMM
$\mathcal{N}(1,1)$，$\alpha_{1}=0.4$，$\mathcal{N}(3,1)$，$\alpha_{2}=0.6$，$x=2$
1. 计算 $x=2$ 处两个高斯分布的概率密度
2. 计算样本属于第一个高斯分布的后验概率

四、决策树
1. 请简述三种决策树算法使用到的属性划分指标（带公式），并分析其优缺点
2. 请书写 C4.5 算法的伪代码。

五、SVM
给出六个图像，分析每个图像属于哪种 SVM
a) 软间隔线性 SVM，$C=0.1$
b) 软间隔线性 SVM，$C=10$
c) 硬间隔核 SVM，$\mathcal{K}( \boldsymbol{u}, \boldsymbol{v}) = \boldsymbol{u}^T \boldsymbol{v}+( \boldsymbol{u}^T \boldsymbol{v})^2$
d) 硬间隔核 SVM，$\mathcal{K}( \boldsymbol{u}, \boldsymbol{v}) = \exp \left( -\dfrac{1}{4}  \lVert \boldsymbol{u}- \boldsymbol{v} \rVert\right)$
e) 硬间隔核 SVM，$\mathcal{K}(\boldsymbol{u}, \boldsymbol{v}) = \exp \left( -4\lVert  \boldsymbol{u} - \boldsymbol{v} \rVert \right)$
f) 其他

图 1：硬间隔核 SVM，$\mathcal{K}( \boldsymbol{u}, \boldsymbol{v}) = \exp \left( -\dfrac{1}{4} \lVert \boldsymbol{u}- \boldsymbol{v} \rVert\right)$ 
图 2：硬间隔核 SVM，核函数为三次多项式，图为一条三次曲线
图 3：软间隔线性 SVM，$C=0.1$
图 4：软间隔线性 SVM，$C=10$
图 5：硬间隔核 SVM，$\mathcal{K}( \boldsymbol{u}, \boldsymbol{v}) = \boldsymbol{u}^T \boldsymbol{v}+( \boldsymbol{u}^T \boldsymbol{v})^2$，图为一条双曲线
图 6：硬间隔核 SVM，$\mathcal{K}(\boldsymbol{u}, \boldsymbol{v}) = \exp \left( -4\lVert \boldsymbol{u} - \boldsymbol{v} \rVert \right)$

六、PCA
给出 $\lambda_{1}=9,\lambda_{2}=4,\lambda_{3}=1$
1. 计算各特征值对样本方差的贡献率和累计贡献率
2. 如果考虑贡献率 $>0.9$，应该至少保留多少维度
3. 如果样本维度为 $3$，有 $1000$ 个样本，降维为 $2$ 维，求重构误差占样本方差的比。

七、神经网络
1. 给出图像 $3 \times 224 \times 224$，若有 $10$ 个大小为 $7 \times 7$ 的卷积核，padding 为 $3$，步长为 $2$，则经过此卷积层后特征的形状是什么
2. 给定 $4\times4$ 的矩阵和一个卷积核 $\begin{pmatrix}1 & 0 \\ 2 & 1\end{pmatrix}$，无 padding，步长为 $1$，求其经过卷积后再通过 $2\times2$ 的 Max-Pooling 层后的输出
3. 如果输入是一个 $(N, C, W, H)$ 分析 Batch Normalization 和 Layer Normalization 的归一化方式。
# 26机器学习回忆版

# 一、选择题

## 

为什么朴素贝叶斯是”朴素"的\(\)

A\. 认为特征之间相互独立

B\.认为各个训练样本之间相互独立

C\.认为各个类别相互独立

D\.认为每个类别中各个特征相互独立

## 

以下哪一个选项中的模型全部是无监督的（）

A\.线性回归模型和决策树

B\.朴素贝叶斯和线性判别分析

C\.高斯混合模型和主成分分析

D\.

## 生成式与判别式模型

判断以下说法正确的是（）

A\.logistic回归和kmeans聚类属于判别式模型

B\.朴素贝叶斯和高斯混合模型是生成式模型

C\.

D\.决策树重视数据的来源，是生成分支长枝叶的，所以它是生成式模型

## 偏差与方差

模型在训练集上表现良好，测试集表现很差，以下说法错误的是（）

A\.过拟合，把噪声当成样本学习了

B\.经验误差小，期望误差大

C\.从偏差方差分解角度，可能是偏差很大，方差很小

D\.可以引入正则化项，原理是通过牺牲一定的偏差使方差大大降低

## f\(x\)=\-x²\+3x\+5是什么函数？

A\.凸函数

B\.凹函数

C\.既是凸函数也是凹函数

D\.均不是

## 

使用em算法更新高斯混合模型，模型中的隐变量是？

A\.

B\.用来指示属于哪个高斯聚类的指示变量

C\.各个高斯聚类的均值向量

D\.协方差矩阵的特征值





# 二、简答题



## 讨论二分类方法推广至多分类

1. 阐述多分类方法OvO和OvR的策略和思想

2. 结合两者训练分类器的数目，阐述两者潜在的优缺点



## 决策树

给定数据集，数据特征为x、y，标签为f。计算并判断ID3决策树下一个节点用特征x还是y进行分类

## 支持向量机

软间隔下，给定g\_i为函数距离，$\zeta_{i}＝max（0，1-g_i)$，惩罚项$C\sum \zeta_i$，C=2

g1=1\.4，g2=1，g3=0\.6，g4＝\-0\.2，g5=2\.1

1\.算各个$\zeta_i$

2\.算惩罚项

3\.哪些未违反margin约束，哪些分类正确但落在margin内，哪些分类错误

4\.C的变化对分类的影响（变大变小都讨论）



## k\-means聚类

x1 = \(1,1\) , x2 = \(2,1\) , x3 = \(4,3\),x4 = \(5,4\) ， 假定初始中心 u1 = x1 = \(1,1\) ， u2 = x2 = \(2,1\) 

1. 写出 k\-means 聚类算法的伪代码（中文即可）

2. 写出进行一次迭代后聚类的结果，包括聚类情况和聚类中心坐标

3. 给出最后迭代稳定的结果，包括聚类情况和聚类中心坐标



## 线性模型

在测定电阻θ的实验中，得到数据\(1,2\.1\),\(2,3\.9\),\(3,6\)

1. 建立线性模型

2. 设置损失函数，并阐述损失函数的数学意义

3. 用闭式解求解该问题

4. 使用梯度下降法求解，初始值 $\theta_{0} = 1$ ， 学习率 $\eta = 0.01$ ， 写出迭代一次后θ的值



## 神经网络

现有一个用于二分类的前馈神经网络。输入为二维特征 $\mathbf{x}=(x_1,x_2)$
输出为预测概率 \(o\)。网络包含两个隐藏层，每层均只有一个神经元，其前向传播过程如下：

$a=w_{11}x_1+w_{12}x_2$  $b=\sigma(a)$ $c=w_2b$ $d=\sigma(c)$  $e=w_3d$ $o=\sigma(e)$

对于单个训练样本 $\big((x_1,x_2),y\big)$，其中 $y\in{[0,1]}$，采用梯度下降法更新网络参数。

1. 根据上述前向传播关系，画出该神经网络的结构图。

2. 若隐藏层激活函数 $\sigma(\cdot)$为线性激活函数，损失函数为均方差损失函数，请推导权重 $w_{11}$ 的更新公式。

3. 若隐藏层激活函数 $\sigma(\cdot)$为 sigmoid激活函数，损失函数为交叉熵函数，请推导权重 $w_{12}$  的更新公式。



# 




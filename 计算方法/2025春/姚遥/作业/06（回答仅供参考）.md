# 7
> 设 $\boldsymbol{A}$ 是对称正定矩阵，经过 Gauss 消去法一步后，$\boldsymbol{A}$ 约化为 $\begin{pmatrix}a_{11}& \boldsymbol{a}_{1}^{\mathrm{T}}\\ \boldsymbol{0}& \boldsymbol{A}_{2}\end{pmatrix}$，其中
> $$\boldsymbol{A} = (a_{ij})_{n},\quad \boldsymbol{A}_{2}=(a_{ij}^{(2)})_{n - 1}.$$
> 证明：(1) $\boldsymbol{A}$ 的对角元素 $a_{ii}>0\ (i = 1,2,\cdots,n)$；
> (2) $\boldsymbol{A}_{2}$ 是对称正定矩阵；
> (3) $a_{ii}^{(2)}\leqslant a_{ii}\ (i = 2,3,\cdots,n)$；
> (4) $\boldsymbol{A}$ 的绝对值最大的元素必在对角线上；
> (5) $\max_{2\leqslant i,j\leqslant n}|a_{ij}^{(2)}|\leqslant\max_{2\leqslant i,j\leqslant n}|a_{ij}|$；
> (6) 从 (2)、(3)、(5) 推出，如果 $|a_{ij}|<1$，则对于所有 $k$，$|a_{ij}^{(k)}|<1$。

解：(1) 假设存在 $i$，使得 $a_{ii} \le 0$，则令向量 $\boldsymbol{x}$ 满足 $x_j=\begin{cases}1 & j=i\\ 0 & j \neq i\end{cases}$ ，则有 $\boldsymbol{x}^T\boldsymbol{Ax} = a_{ii} \le 0$，与 $\boldsymbol{A}$ 是对称正定矩阵矛盾！因此 $\boldsymbol{A}$ 的对角元素 $a_{ii} > 0$。
(2) 由 Gauss 消元流程，有：
$$
a^{(2)}_{ij} = a_{ij} - \dfrac{a_{i1}}{a_{11}} a_{1j} \quad i,j=2,3,\dots,n
$$
因此， $a_{ji}^{(2)}=a_{ji} - \dfrac{a_{j1}}{a_{11}} a_{1 i}$，而 $a_{ij}=a_{ji}, a_{i1}=a_{1i}, a_{j1}=a_{1j}$。所以，$a_{ij}^{(2)}=a_{ji}^{(2)}$。故 $\boldsymbol{A}_2$ 仍为对称矩阵。
Gauss 消元的第一步可看作对 $\boldsymbol{A}$ 的合同变换：$\begin{pmatrix}a_{11}& \boldsymbol{a}_{1}^{\mathrm{T}}\\ \boldsymbol{0}& \boldsymbol{A}_{2}\end{pmatrix} = \boldsymbol{LAL}^T$，因此约化后的矩阵仍为正定矩阵。若取 $\boldsymbol{x} \in \mathbf{R}^{n-1}$，$\boldsymbol{x}'=\begin{pmatrix}0 & \boldsymbol{x}\end{pmatrix}^T$，则 $\forall \boldsymbol{x}' \neq \boldsymbol{0},(\boldsymbol{x}')^T\begin{pmatrix}a_{11}& \boldsymbol{a}_{1}^{\mathrm{T}}\\ \boldsymbol{0}& \boldsymbol{A}_{2}\end{pmatrix} \boldsymbol{x}' = \boldsymbol{x}^T \boldsymbol{A}_2 \boldsymbol{x} > 0$，因此 $\boldsymbol{A}_2$ 仍为正定矩阵。
(3) 由 (2) 得，$a_{ii}^{(2)}=a_{ii}- \dfrac{a_{i1}a_{1i}}{a_{11}}$，而由 $\boldsymbol{A}$ 对称性，有 $a_{i1}=a_{1i}$。故 $a_{ii}^{(2)}=a_{ii} - \dfrac{a_{i1}^2}{a_{11}} \le a_{ii}$。
(4) 假设 $\boldsymbol{A}$ 绝对值最大的元素为 $a_{ij}=a_{ji} (i \neq j)$。若 $a_{ij} \ge 0$，则令向量 $\boldsymbol{x}$ 满足 $x_k=\begin{cases}1 & k=i\\ -1 & k=j\\ 0 & \text{otherwise}\end{cases}$，则有 $\boldsymbol{x}^T \boldsymbol{Ax} = a_{ii}+a_{jj}-2a_{ij} = (a_{ii}-a_{ij})+(a_{jj}-a_{ij}) \le 0$，与 $\boldsymbol{A}$ 是正定矩阵矛盾！若 $a_{ij}<0$，取 $\boldsymbol{x}'$ 满足 $x'_k = \begin{cases}1 & k=i \text{ or } k=j \\ 0 & \text{otherwise}\end{cases}$ 同理可得 $\boldsymbol{x}^T \boldsymbol{Ax}=(a_{ii}+a_{ij})+(a_{jj}+a_{ij}) < 0$，与 $\boldsymbol{A}$ 正定矛盾！
综上，$\boldsymbol{A}$ 绝对值最大的元素一定出现在对角线上。
(5) 由 (2) 知 $\boldsymbol{A}_2$ 为对称正定矩阵，因此与 (1) 同理，有 $a^{(2)}_{ii} > 0$；与 (4) 同理，有 $\boldsymbol{A}_2$ 绝对值最大的元素也一定出现在对角线上。又由 (2) 可知，$a_{ii}^{(2)} \le a_{ii}$。因此有
$$
\begin{align}
&\max_{2 \le i,j \le n} |a^{(2)}_{ij}| = \max_{2 \le i \le n} |a^{(2)}_{ii}|  =\max_{2 \le i \le n} a^{(2)}_{ii} \\
\le& \max_{2 \le i \le n} a_{ii} = \max_{2 \le i \le n} |a_{ii}| =\max_{2 \le i,j \le n} |a_{ij}|
\end{align}
$$
故有 $\max_{2\leqslant i,j\leqslant n}|a_{ij}^{(2)}|\leqslant\max_{2\leqslant i,j\leqslant n}|a_{ij}|$；
(6) 由 (2) 可知，$\boldsymbol{A}_k$ 均为对称正定矩阵。由 (5) 可知，$\max_{k+1\leqslant i,j\leqslant n}|a_{ij}^{(k+1)}|\leqslant\max_{k+1\leqslant i,j\leqslant n}|a^{(k)}_{ij}|$。再由于 $|a_{ij}| < 1$，即 $\max_{1 \le i,j \le n}|a_{ij}| < 1$，可得对于所有 $k$，有 $\max_{k+1 \le i,j \le n}|a_{ij}^{(k)}| \le \max_{1\le i,j \le n}|a_{ij}| <1$，也即 $|a_{ij}^{(k)}| < 1\quad(k=1,2,\dots,n)$。
# 15
> 下述矩阵能否分解为 $\boldsymbol{LU}$（其中 $\boldsymbol{L}$ 为单位下三角阵，$\boldsymbol{U}$ 为上三角阵）？若能分解，那么分解是否唯一？
>$$\mathbf{A}=\begin{pmatrix} 1 & 2 & 3 \\ 2 & 4 & 1 \\ 4 & 6 & 7 \end{pmatrix}, \mathbf{B}=\begin{pmatrix} 1 & 1 & 1 \\ 2 & 2 & 1 \\ 3 & 3 & 1 \end{pmatrix}, \mathbf{C}=\begin{pmatrix} 1 & 2 & 6 \\ 2 & 5 & 15 \\ 6 & 15 & 46 \end{pmatrix}$$

对 $\boldsymbol{A}$，待定系数得如下方程组：
$$
\begin{cases}
a_{1i} = u_{1i} & i=1,2,3 \\
a_{21} = u_{11}l_{21} \\
a_{2i} = u_{1i}l_{21}+u_{2i} & i=2,3 \\
a_{31} = u_{11}l_{31}  \\
a_{32} = u_{12}l_{31}+u_{22}l_{32}  \\
a_{33} = u_{13}l_{31}+u_{23}l_{32}+u_{33}
\end{cases}
$$
求解过程中出现 $u_{12}=2, l_{31}=4, u_{22}=0, a_{32}=6$，此时 $l_{32}$ 前系数为 $0$，方程无解。故 $\boldsymbol{A}$ 无法分解为 $\boldsymbol{LU}$。
对 $\boldsymbol{B}$，同理解方程组，过程中出现 $u_{12}=1, l_{31}=3, u_{22}=0, b_{32}=3$，此时 $l_{32}$ 前系数为 $0$，方程有无穷多解。$\boldsymbol{B}$ 可以分解为 $\boldsymbol{LU}$，但分解不唯一。
对 $\boldsymbol{C}$，求解得：
$$
\boldsymbol{C} = \begin{pmatrix}1&0&0\\2&1&0\\6&3&1\end{pmatrix} 
\begin{pmatrix}1&2&6\\0&1&3\\0&0&1\end{pmatrix}
$$
即 $\boldsymbol{L}=\begin{pmatrix}1&0&0\\2&1&0\\6&3&1\end{pmatrix}, \boldsymbol{U} = \begin{pmatrix}1&2&6\\0&1&3\\0&0&1\end{pmatrix}$。待定系数为线性方程组的解，因此是唯一的。

# 21
> 设 $\boldsymbol{A} \in \mathbf{R}^{n\times n}$ 为对称正定矩阵，定义 $\lVert \boldsymbol{x} \rVert_{ \boldsymbol{A}} = (\boldsymbol{Ax}, \boldsymbol{x})^{\frac12}$，试证明 $\lVert \boldsymbol{x} \rVert_{\boldsymbol{A}}$ 为 $\mathbf{R}^n$ 上向量的一种范数。

解：正定性：由 $\boldsymbol{A}$ 为对称正定矩阵可知，$\lVert \boldsymbol{x} \rVert_{ \boldsymbol{A}} = \sqrt{ (\boldsymbol{Ax})^T \boldsymbol{x} }=\sqrt{ \boldsymbol{x}^T \boldsymbol{A} \boldsymbol{x} } \ge 0$，$\lVert \boldsymbol{x} \rVert_{\boldsymbol{A}}=0 \iff \boldsymbol{x} = \boldsymbol{0}$
齐次性：$\forall \alpha \in \mathbf{R},\lVert \alpha\boldsymbol{x} \rVert_{\boldsymbol{A}}=\sqrt{ (\alpha \boldsymbol{x})^T \boldsymbol{A} (\alpha \boldsymbol{x}) }=\sqrt{ \alpha^2(\boldsymbol{x}^T \boldsymbol{Ax}) }=|\alpha| \sqrt{ \boldsymbol{x}^T \boldsymbol{Ax} }=|\alpha| \lVert \boldsymbol{x} \rVert_{\boldsymbol{A}}$
三角不等式：$\lVert \boldsymbol{x} + \boldsymbol{y} \rVert_{\boldsymbol{A}}^2 = (\boldsymbol{x} + \boldsymbol{y})^T \boldsymbol{A} (\boldsymbol{x} + \boldsymbol{y})=\boldsymbol{x}^T \boldsymbol{A} \boldsymbol{x} + \boldsymbol{x}^T \boldsymbol{A} \boldsymbol{y} +\boldsymbol{y}^T \boldsymbol{A} \boldsymbol{x} + \boldsymbol{y}^T \boldsymbol{A} \boldsymbol{y}$
而 $(\lVert \boldsymbol{x} \rVert_{\boldsymbol{A}} + \lVert \boldsymbol{y} \rVert_{\boldsymbol{A}})^2=\lVert \boldsymbol{x} \rVert_{\boldsymbol{A}}^2+\lVert \boldsymbol{y} \rVert_{\boldsymbol{A}}^2+2\lVert \boldsymbol{x} \rVert_{\boldsymbol{A}}\lVert \boldsymbol{y} \rVert_{\boldsymbol{A}}=\boldsymbol{x}^T \boldsymbol{A} \boldsymbol{x}+\boldsymbol{y}^T \boldsymbol{A} \boldsymbol{y}+2\sqrt{(  \boldsymbol{x}^T \boldsymbol{A} \boldsymbol{x})(\boldsymbol{y}^T \boldsymbol{A} \boldsymbol{y})}$ 
下面先证：$\boldsymbol{x}^T \boldsymbol{Ay} \le \sqrt{ \boldsymbol{x}^T \boldsymbol{Ax} } \sqrt{ \boldsymbol{y}^T \boldsymbol{Ay} }$
设参数 $t \in \mathbf{R}$，构造二次型 $(\boldsymbol{x} + t \boldsymbol{y})^T \boldsymbol{A}(\boldsymbol{x}+ t \boldsymbol{y}) \ge 0$，则有 $\boldsymbol{x}^T \boldsymbol{A} \boldsymbol{x} + 2t \boldsymbol{x}^T \boldsymbol{Ay}+ t^2\boldsymbol{y}^T \boldsymbol{A} \boldsymbol{y} \ge 0$。由于对于任意 $t$，此式均要成立。故有 $\Delta = (2 \boldsymbol{x}^T \boldsymbol{Ay})^2-4(\boldsymbol{y}^T \boldsymbol{Ay})(\boldsymbol{x}^T \boldsymbol{Ax}) \le 0$，也即 $(\boldsymbol{x}^T \boldsymbol{Ay})^2 \le (\boldsymbol{y}^T \boldsymbol{Ay})(\boldsymbol{x}^T \boldsymbol{Ax})$。故有 $\boldsymbol{x}^T \boldsymbol{Ay} \le \sqrt{ \boldsymbol{x}^T \boldsymbol{Ax} } \sqrt{ \boldsymbol{y}^T \boldsymbol{Ay} }$。
因此 $\boldsymbol{y}^T \boldsymbol{A} \boldsymbol{x} + \boldsymbol{y}^T \boldsymbol{A} \boldsymbol{y} \le 2\sqrt{(  \boldsymbol{x}^T \boldsymbol{A} \boldsymbol{x})(\boldsymbol{y}^T \boldsymbol{A} \boldsymbol{y})}$，故 $\lVert \boldsymbol{x} + \boldsymbol{y} \rVert_{\boldsymbol{A}}^2 \le (\lVert \boldsymbol{x} \rVert_{\boldsymbol{A}} + \lVert \boldsymbol{y} \rVert_{\boldsymbol{A}})^2$。
综上，$\lVert \boldsymbol{x} \rVert_{\boldsymbol{A}}$ 为 $\boldsymbol{R}^n$ 上的一种范数。
# 1
> 设方程组 $\begin{cases}5x_1 + 2x_2+x_3=-12\\-x_1 + 4x_2+2x_3 = 20\\2x_1-3x_2 + 10x_3=3\end{cases}$
> (1) 考察用 Jacobi 迭代法、Gauss - Seidel 迭代法解此方程组的收敛性；
> (2) 用 Jacobi 迭代法、Gauss - Seidel 迭代法解此方程组，要求当 $\|x^{(k + 1)}-x^{(k)}\|_{\infty}<10^{-4}$ 时迭代终止。

解：(1) 将方程组改写成：
$$
\begin{cases}
x_{1}=\dfrac{1}{5}(-2x_{2}-x_{3}-12) \\
x_{2}=\dfrac{1}{4}(x_{1}-2x_{3}+20) \\
x_{3}=\dfrac{1}{10}(-2x_{1}+3x_{2}+3)
\end{cases}
$$
因此有 $\boldsymbol{B}_0=\begin{bmatrix}0 & -\dfrac{2}{5} & -\dfrac{1}{5}\\ \dfrac{1}{4} & 0 & -\dfrac{1}{2} \\ -\dfrac{1}{5} & \dfrac{3}{10} & 0\end{bmatrix}, \boldsymbol{f}=\begin{bmatrix}-\dfrac{12}{5} \\ 5 \\ \dfrac{3}{10}\end{bmatrix}$
由此解得 $\boldsymbol{B}_0$ 的特征值为 $\lambda_{1}=-0.215,\lambda_{2}=0.104+0.495i,\lambda_{3}=0.104-0.495i$，因此 $\rho(\boldsymbol{B}_0) < 1$，故使用 Jocobi 迭代法是收敛的。
由 Gauss - Seidel 迭代法，$\boldsymbol{G}=\begin{bmatrix} 0 & -0.4 & -0.2 \\ 0 & -0.1 & -0.55 \\ 0 & -0.05 & -0.125\end{bmatrix}$，由此解得 $\boldsymbol{G}$ 的特征值为 $\lambda_{1}=0,\lambda_{2}=-0.113+0.165i,\lambda_{3}=-0.113+0.165i$，因此 $\rho(\boldsymbol{G})<1$，故使用 Gauss - Seidel 迭代法是收敛的。
(2) 取 $x_{0}=\begin{bmatrix}0&0&0\end{bmatrix}^T$，则由 Jocobi 迭代法得，$x_1 = \begin{bmatrix}-2.40000 & 5.00000 & 0.30000\end{bmatrix}^T$，$x_2 = \begin{bmatrix}-4.46000 & 4.25000 & 2.28000\end{bmatrix}^T$，$\dots$，$x_{17} = \begin{bmatrix}-4.00004 & 2.99997 & 2.00003\end{bmatrix}^T$，$x_{18} = \begin{bmatrix}-4.00000 & 2.99997 & 2.00000\end{bmatrix}^T$。因此结果为 $x=\begin{bmatrix}-4.00000 & 2.99997 & 2.00000\end{bmatrix}^T$。
同理，由 Gauss - Seidel 迭代法得，$x_{1} = \begin{bmatrix}-2.40000 & 4.40000 & 2.10000\end{bmatrix}^T$，$x_{2} = \begin{bmatrix}-4.58000 & 2.80500 & 2.05750\end{bmatrix}^T$，$\dots$，$x_{7} = \begin{bmatrix}-3.99997 & 3.00007 & 2.00002\end{bmatrix}^T$，$x_{8} = \begin{bmatrix}-4.00003 & 2.99998 & 2.00000\end{bmatrix}^T$。因此结果为 $x=\begin{bmatrix}-4.00003 & 2.99998 & 2.00000\end{bmatrix}^T$。
# 5
> 设方程组
> $$(1) \begin{cases}x_1 + 0.4x_2 + 0.4x_3 = 1\\0.4x_1 + x_2 + 0.8x_3 = 2\\0.4x_1 + 0.8x_2 + x_3 = 3\end{cases} \quad (2) \begin{cases}x_1 + 2x_2 - 2x_3 = 1\\x_1 + x_2 + x_3 = 1\\2x_1 + 2x_2 + x_3 = 1\end{cases}$$
> 试考察解此方程组的 Jacobi 迭代法及 Gauss - Seidel 迭代法的收敛性。 

解：(1) 对于 Jacobi 迭代法，$\boldsymbol{B}_0$ 的特征值为 $0.293, -1.093, 0.8$，因此 $\rho(\boldsymbol{B}_0)>1$，从而迭代法不收敛。对于 Gauss-Seidel 迭代法，$\boldsymbol{G}$ 的特征值为 $0, 0.204, 0.628$，因此 $\rho(\boldsymbol{G})<1$，从而迭代法收敛。
(2) 对于 Jacobi 迭代法，$\boldsymbol{B}_0$ 的特征值为 $-5.4\times 10^{-6}+9.4 \times 10^{-6}i, -5.4\times 10^{-6}-9.4 \times 10^{-6}i, 1.08 \times 10^{-5}$，因此 $\rho(\boldsymbol{B}_0)<1$，从而迭代法收敛。对于 Gauss-Seidel 迭代法，$\boldsymbol{G}$ 的特征值为 $0,2,2$，因此 $\rho(\boldsymbol{G})>1$，从而迭代法不收敛。
# 8
> 设方程组
> $$\begin{cases} x_1 - \frac{1}{4}x_3 - \frac{1}{4}x_4 = \frac{1}{2} \\ x_2 - \frac{1}{4}x_3 - \frac{1}{4}x_4 = \frac{1}{2} \\ -\frac{1}{4}x_1 - \frac{1}{4}x_2 + x_3 = \frac{1}{2} \\ -\frac{1}{4}x_1 - \frac{1}{4}x_2 + x_4 = \frac{1}{2} \end{cases}$$
> (1) 求解此方程组的 Jacobi 迭代法的迭代矩阵 $\boldsymbol{B}_0$ 的谱半径；
> (2) 求解此方程组的 Gauss - Seidel 迭代法的迭代矩阵 $\boldsymbol{B}_0$ 的谱半径；
> (3) 考察解此方程组的 Jacobi 迭代法及 Gauss - Seidel 迭代法的收敛性。

解：(1) 由 Jacobi 迭代法，$\boldsymbol{B}_0=\begin{bmatrix}0.00 & 0.00 & 0.25 & 0.25 \\ 0.00 & 0.00 & 0.25 & 0.25 \\ 0.25 & 0.25 & 0.00 & 0.00 \\ 0.25 & 0.25 & 0.00 & 0.00 \\ \end{bmatrix}$，从而有 $\boldsymbol{B}_0$ 特征值为 $-0.5, 0.5, 0, 0$，故其谱半径 $\rho(\boldsymbol{B}_0)=0.5$
(2) 由 Gauss-Seidel 迭代法，$\boldsymbol{G}=\begin{bmatrix}0.000 & 0.000 & 0.250 & 0.250 \\ 0.000 & 0.000 & 0.250 & 0.250 \\ 0.000 & 0.000 & 0.125 & 0.125 \\ 0.000 & 0.000 & 0.125 & 0.125 \\ \end{bmatrix}$，从而有 $\boldsymbol{G}$ 特征值为 $0, 0, 0.25, 0$，故其谱半径 $\rho(\boldsymbol{G})=0.25$
(3) 由于 $\rho(\boldsymbol{B}_0) < 1$ 且 $\rho(\boldsymbol{G})<1$，故 Jacobi 迭代法和 Gauss-Seidel 迭代法均收敛。
# 南京大学本科期末考试试卷（B卷）

**课程名称：** 《高级程序设计》
**学年学期：** 2025-2026 学年 第 1 学期
**开课单位：** 智能软件与工程学院
**考试方式：** 闭卷
**考试时长：** 120 分钟
**总分：** 100 分

---

## 一、简答题（共 4 题，共 20 分）

### 1. 什么是函数重载？函数重载需要满足哪些条件？（5 分）

### 2. 什么是模板？函数模板与类模板有什么区别？（5 分）

### 3. 在面向对象程序设计中，继承有哪些类型？继承在代码复用中起什么作用？（5 分）

### 4. 什么是接口类？在 C++ 中如何实现接口类？（5 分）

---

## 二、程序分析题（共 4 题，共 40 分）

（请写出必要的分析过程，若出现未定义行为 Undefined Behavior 导致输出结果不确定，请将不确定的结果用 xxx 代替。）

### 1.（10 分）阅读下面程序，回答问题

**代码：**

```cpp
#include <iostream>
using namespace std;

class Student {
    string name;
    int age;
public:
    Student(string n, int a) : name(n), age(a) {
        cout << "1: " << name << endl;
    }
    Student(const Student &s) {
        name = s.name;
        age = s.age;
        cout << "2: " << name << endl;
    }
    ~Student() {
        cout << "3: " << name << endl;
    }
    void display() {
        cout << name << " " << age << "岁" << endl;
    }
};

Student createStudent(string n, int a) {
    Student s(n,a);
    return s;
}

int main() {
    Student s1("Alice", 18);
    Student s2 = s1;
    Student s3("Bob", 19);
    Student s4 = createStudent("Cindy", 20);
    s1.display();
    s2.display();
    s3.display();
    s4.display();
    return 0;
}
```

**问题：**

1. 写出程序的完整输出

2. 哪些语句调用了拷贝构造函数？如果 Student 类中没有定义拷贝构造函数，程序还能正常运行吗？会有什么不同？

### 2.（10 分）阅读下面程序，回答问题。

**代码：**

```cpp
#include <iostream>
using namespace std;

class Data {
    int value;
public:
    Data(int v = 0) : value(v) {}
    Data operator+(const Data &d) {
        return Data(value + d.value);
    }
    Data operator-(const Data &d) {
        return Data(value - d.value);
    }
    Data operator++() {
        ++value;
        return *this;
    }
    Data operator++(int) {
        Data temp = *this;
        value++;
        return temp;
    }
    friend ostream& operator<<(ostream& os, const Data &d) {
        os << d.value;
        return os;
    }
};

int main() {
    Data d1(10);
    Data d2(3);

    Data d3 = d1 + d2;
    Data d4 = d1 - d2;
    Data d5 = ++d1;
    Data d6 = d2++;
    
    cout << "d1 = " << d1 << endl;
    cout << "d2 = " << d2 << endl;
    cout << "d3 = " << d3 << endl;
    cout << "d4 = " << d4 << endl;
    cout << "d5 = " << d5 << endl;
    cout << "d6 = " << d6 << endl;
    return 0;
}
```

**问题：**

1. 写出程序运行结果

2. 解释前置++和后置++的区别，并说明为什么后置++要返回临时对象。

3. 如果去掉 friend，operator<< 还能正确访问 value 吗？为什么？

4. 假设要实现 d1+5（Data+int），应该如何添加运算符重载函数？写出代码。

### 3.（10 分）阅读下面程序，回答问题。

**代码：**

```cpp
#include <iostream>
using namespace std;

class Shape {
public:
    virtual double area() { return 0; }
    void show() { cout << "面积:" << area() << endl; }
    virtual ~Shape() {}
};

class Rectangle : public Shape {
    double w, h;
public:
    Rectangle(double w, double h) : w(w), h(h) {}
    double area() override { return w * h; }
};

class Circle : public Shape {
    double r;
public:
    Circle(double r) : r(r) {}
    double area() override { return 3.14 * r * r; }
};

int main() {
    Rectangle rect(3, 4);
    Circle cir(5);

    Shape* shapes[2];
    shapes[0] = &rect;
    shapes[1] = &cir;

    for (int i = 0; i < 2; i++) {
        shapes[i]->show();
    }
    
    Shape s;
    cout << "Shape 面积:" << s.area() << endl;

    return 0;
}
```

**问题：**

1. 写出程序的运行结果。

2. 为什么 show() 函数中调用 area() 能正确计算出不同图形的面积？

3. 如果把 Shape 类中的 virtual 去掉，输出会有什么变化？为什么。

### 4.（10 分）阅读下面程序，回答问题。

**代码：**

```cpp
#include <iostream>
#include <stdexcept>
using namespace std;

int step = 0;
void validate(int n) {
    step = 1;
    if (n < 0) {
        throw runtime_error("negative input");
    }
    step = 2;
}

void process(int n) {
    step = 3;
    try {
        validate(n);
        step = 4;
        cout << "process: success" << endl;
    } catch (const exception& e) {
        step = 5;
        cout << "process:" << e.what() << endl;
        // add `throw;` here
    }
    step = 6;
    cout << "process: end" << endl;
}

int main() {
    try {
        step = 0;
        process(-5);
        step = 7;
        cout << "main: success" << endl;
    } catch (const exception &e) {
        step = 8;
        cout << "main: " << e.what() << endl;
    }
    cout << "Final step: " << step << endl;
    return 0;
}
```

**问题：**

1. 写出程序的完整输出结果，并给出最终 step 的值。分析 process 函数中 `step = 4;` 这行代码是否会被执行？为什么？

2. 如果在 process 函数的 catch 块末尾添加语句 `throw;` 程序的输出和最终 step 的值会发生什么变化？请解释异常传播机制的改变。

---

## 三、编程题（共 2 题，共 40 分）

### 1.（20 分）请设计一个分数类 Fraction，支持分数的构造、加法运算和相等比较。

**要求：**

1. 根据 //TODO 注释，在代码中补全 Fraction 类的实现。

2. 分数采用"分子/分母"形式存储，构造函数中需自动约分（用于计算最大公约数的函数已提供），并保持分母为正。

3. 重载 + 运算符实现分数加法：a/b + c/d = (ad+bc)/bd，结果要自动约分。

4. 重载 == 运算符判断两个分数是否相等（比较约分后的值）。

5. 重载输出流运算符 <<，以 分子/分母 格式输出（若分母为 1 则只输出分子）。

**需要补充的代码：**

```cpp
// 辅助函数：求最大公约数（已提供，可直接调用）
class Fraction {
private:
    int numerator;   // 分子
    int denominator; // 分母（保持为正）
    
    int gcd(int a, int b) {
        while (b) { int t = b; b = a % b; a = t; }
        return a;
    }
    
    void reduce() {  // 辅助函数：约分（已提供，可直接调用）
        if (denominator < 0) {  // 保证分母为正
            numerator = -numerator;
            denominator = -denominator;
        }
        int g = gcd(abs(numerator), denominator);
        numerator /= g;
        denominator /= g;
    }

public:
    // TODO 1: 分子分母构造函数
    Fraction(int numerator, int denominator);
    // 编者注：这里只提供了两个参数的构造函数，不带默认值，但是后面用到 Fraction f2(3); 这样的单参数构造，什么鬼
    
    // TODO 2: 加法运算符重载
    Fraction operator+(const Fraction &rhs) const;
    
    // TODO 3: 相等比较运算符重载
    bool operator==(const Fraction &rhs) const;
    
    // TODO 4: 输出流运算符重载
    friend ostream& operator<<(ostream &os, const Fraction &f);
};
```

**你编写的代码应当能通过以下测试：**

```cpp
int main() {
    // 测试 1: 构造函数与约分
    Fraction f1(2, 4);    // 自动约分为 1/2
    Fraction f2(3);       // 整数构造 3/1
    Fraction f3;          // 默认构造 0/1
    cout << f1 << ", " << f2 << ", " << f3 << endl;   // 整数输出: 1/2, 3, 0
    
    // 测试 2: 加法运算（结果自动约分）
    Fraction f4(1, 3);      // 1/3
    Fraction f5(1, 6);      // 1/6
    Fraction f6 = f4 + f5;  // 1/3 + 1/6 = 1/2
    cout << f6 << endl;     // 输出: 1/2
    
    // 测试 3: 相等比较（约分后值相同即相等）
    Fraction f7(2, 4);      // 1/2
    Fraction f8(3, 6);      // 1/2
    cout << boolalpha << (f7 == f8) << endl;  // 输出: true
    
    // 测试 4: 链式加法
    Fraction f9 = f1 + f4 + f5;  // 1/2 + 1/3 + 1/6 = 1/1
    cout << f9 << endl;         // 输出: 1
    return 0;
}
```

### 2.（20 分）对象仓库与引用计数指针

请设计并实现一个基于引用计数的指针类模板 `SharedRef` 和一个对象仓库容器类模板 `ObjectRepository`。

**要求：**

1. 根据注释补全 `SharedRef` 与 `ObjectRepository` 的实现。

2. 实现引用计数指针的基本生命周期管理（构造、拷贝、赋值、析构）。

3. 实现仓库容器的基本管理功能（登记对象、清理节点）。

4. 实现泛型数据处理操作（transform 转换、collect_if 条件收集）。

你需要注意的是：

1. `SharedRef` 需要正确维护引用计数，当引用计数降为 0 时释放对象。

2. `ObjectRepository` 内部维护一个**单向链表**结构，每个节点存储一个 `SharedRef`。

3. `main` 函数已经提供，用于测试你的实现。

4. 只能在已有框架中补充代码，不允许删除或修改原有结构。


```cpp
#include <iostream>
#include <functional>
#include <vector>
#include <cassert>
#include <string> // 编者注：原题中没有，这里应该要补上

// 前向声明
template<typename T> class ObjectRepository;
template<typename T> class SharedRef;

// 引用计数控制结构
struct RefCounter {
    int ref;
    RefCounter() : ref(1) {}
};

// TODO 1: 实现引用计数指针（12 分）
template<typename T>
class SharedRef {
    friend class ObjectRepository<T>;
private:
    T* ptr;
    RefCounter* counter;
    // 辅助函数：减少引用
    void decrease() {
        if (counter) {
            counter->ref--;
            if (counter->ref == 0) {
                delete ptr;
                delete counter;
                std::cout << "Object memory freed." << std::endl;
            }
        }
        ptr = nullptr;
        counter = nullptr;
    }
    
public:
    // 默认构造
    SharedRef() : ptr(nullptr), counter(nullptr) {}
    
    // 从原始指针构造
    explicit SharedRef(T* p) : ptr(p) {
        if (p) counter = new RefCounter();
        else counter = nullptr;
    }

    // TODO 1.1 实现拷贝构造函数：实现浅拷贝，但增加引用计数
    SharedRef(const SharedRef& other) {

    }
    
    // TODO 1.2 实现析构函数
    ~SharedRef() {

    }
    
    // TODO 1.3 实现赋值操作符
    // 提示：注意自赋值检测（this != &other），处理旧资源，接管新资源
    SharedRef& operator=(const SharedRef& other) {

    }
    
    T* operator->() const {
        return ptr;
    }
    
    // TODO 1.4 实现指针访问和解引用操作符（*）
    T& operator*() const {
        // 要求：需检查 ptr 是否为空，为空则触发断言，不允许返回引用
    }

    int ref_count() const { return counter ? counter->ref : 0; }
    bool valid() const { return ptr != nullptr; }
};


// 单向链表节点存储指针
template<typename T>
struct RepoNode {
    SharedRef<T> data;
    RepoNode* next;
    RepoNode(const SharedRef<T>& d) : data(d), next(nullptr) {}
};

// TODO 2: 实现对象仓库（8 分）

template<typename T>
class ObjectRepository {

private:
    RepoNode<T>* head;
    size_t count;
    
public:
    ObjectRepository() : head(nullptr), count(0) {}
    ~ObjectRepository() {
        RepoNode<T>* curr = head;
        while (curr) {
            RepoNode<T>* next = curr->next;
            delete curr;
            curr = next;
        }
    }
    
    // 禁止拷贝容器（为了简化题目，本题不要求实现容器的深拷贝）
    ObjectRepository(const ObjectRepository&) = delete;
    ObjectRepository& operator=(const ObjectRepository&) = delete;
    
    // TODO 2.1: 登记新对象
    void register_object(T* raw) {
        // 提示：构造 SharedRef，创建节点，插入到链表头部
    }

    // TODO 2.2: 实现 transform
    // 将仓库中对象转换为另一种类型并返回 vector
    template<typename U, typename Func>
    std::vector<U> transform(Func f) const {
        std::vector<U> result;
        // 在此补充代码
        return result;
    }
    
    // TODO 2.3: collect_if
    // 收集满足条件的对象副本
    std::vector<T> collect_if(std::function<bool(const T&)> pred) const {
        std::vector<T> result;
        // 在此补充代码
        return result;
    }
    
    void display() const {
        RepoNode<T>* curr = head;
        std::cout << "[ ";
        while (curr) {
            if (curr->data.valid())
                std::cout << *(curr->data) << " ";
            curr = curr->next;
        }
        std::cout << "]" << std::endl;
    }
};
```

**你编写的代码应当能通过以下测试：**
```cpp
// 测试用的简单类
class Item {
public:
    std::string label;
    int code;

    Item(std::string l, int c) : label(l), code(c) {
        std::cout << "Item " << label << " created.\n";
    }
    ~Item() {
        std::cout << "Item " << label << " destroyed.\n";
    }
    friend std::ostream& operator<<(std::ostream& os, const Item& i) {
        os << "<" << i.label << ":" << i.code << ">";
        return os;
    }
};

int main() {
    // 测试智能引用
    {
        SharedRef<Item> r1(new Item("Alpha", 10));
        SharedRef<Item> r2 = r1;
        std::cout << "Ref count: " << r1.ref_count() << std::endl;
        SharedRef<Item> r3;
        r3 = r2;
        std::cout << "Ref count: " << r1.ref_count() << std::endl;
        std::cout << "Access: " << r3->label << std::endl;
    }
    std::cout << "-----------" << std::endl;

    ObjectRepository<Item> repo;
    repo.register_object(new Item("A", 3));
    repo.register_object(new Item("B", 7));
    repo.register_object(new Item("C", 2));
    repo.register_object(new Item("D", 5));
    repo.display();

    // transform
    auto vals = repo.transform<int>([](const Item& i) {
        return i.code * 2;
    });
    std::cout << "Transform result: ";
    for (auto v : vals) std::cout << v << " ";
    std::cout << std::endl;

    // collect_if
    auto selected = repo.collect_if([](const Item& i) {
        return i.code >= 4;
    });
    std::cout << "Collect result: ";
    for (auto &x : selected)
        std::cout << x << " ";
    std::cout << std::endl;
    return 0;
}
```